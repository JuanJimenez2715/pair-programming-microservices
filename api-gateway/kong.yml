_format_version: "3.0"

services:
  - name: ms-auth
    url: http://ms-auth:3000
    routes:
      - name: auth-route
        paths:
          - /api/auth
        strip_path: false

  - name: ms-pairing
    url: http://ms-pairing:3000
    routes:
      - name: pairing-route
        paths:
          - /api/sessions
        strip_path: false
        plugins:
          - name: proxy-cache
            config:
              strategy: memory
              cache_ttl: 10
              content_type:
                - "application/json"
              request_method:
                - GET

  - name: ms-analytics
    url: http://ms-analytics:4000
    routes:
      - name: analytics-route
        paths:
          - /api/analytics
        strip_path: false

  - name: ms-editor
    url: http://ms-editor:3000
    routes:
      - name: editor-route
        paths:
          - /api/editor
        strip_path: false
        plugins:
          - name: rate-limiting
            config:
              minute: 30  # Stricter limit for AI Syncs/Editor calls
              policy: local

  - name: ms-ai
    url: http://ms-ai:8080
    routes:
      - name: ai-route
        paths:
          - /api/ai
        strip_path: false

  - name: ms-evaluation
    url: http://ms-evaluation:3000
    routes:
      - name: evaluation-route
        paths:
          - /api/evaluations
        strip_path: false

  - name: ms-exercises
    url: http://ms-exercises:3000
    routes:
      - name: exercises-route
        paths:
          - /api/exercises
        strip_path: false

  - name: ms-notifications
    url: http://ms-notifications:3000
    routes:
      - name: notifications-route
        paths:
          - /api/notifications
        strip_path: false

  - name: ms-reports
    url: http://ms-reports:3000
    routes:
      - name: reports-route
        paths:
          - /api/reports
        strip_path: false

  - name: ms-metrics
    url: http://ms-metrics:3000
    routes:
      - name: metrics-route
        paths:
          - /api/metrics
        strip_path: false

plugins:
  - name: cors
    config:
      origins:
        - '*'
      methods:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
        - OPTIONS
      headers:
        - Accept
        - Accept-Version
        - Content-Length
        - Content-MD5
        - Content-Type
        - Date
        - Authorization
      exposed_headers:
        - X-Trace-ID
      credentials: true
      max_age: 3600

  - name: rate-limiting
    config:
      minute: 200
      policy: local

  - name: correlation-id
    config:
      header_name: X-Trace-ID
      generator: uuid
      echo_downstream: true
